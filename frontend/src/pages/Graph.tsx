import { Typography } from 'antd'

/** Placeholder; the global overview graph lands in Task 22. */
export default function Graph() {
  return (
    <div style={{ padding: 24 }}>
      <Typography.Text type="secondary">Graph（Task 22 实现）</Typography.Text>
    </div>
  )
}
